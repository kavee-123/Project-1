

const setDefault = (elements) => {
    elements.forEach(element => {
        element.style.border = "1px solid #ced4da";
        element.style.border = "1px solid #ced4da";
        element.style.border = "1px solid #ced4da";


        // If use bootsrap classes,
        // element.classList.remove("is-valid");
        // element.classList.remove("is-invalid");
    
    });
}